# Kotlib Project Creator

## Usage
* Start project with `IDEA`
* Use `Main.kt` as a main script
* When done use `cli` command:
```bash
gradle cli
```

## CLI Usage
```bash
kotproj project_name com.company
```